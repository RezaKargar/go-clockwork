# Contributing

## Development

- Run `go test ./...` before opening a PR.
- Keep commits in conventional format (`feat:`, `fix:`, `test:`, `docs:`, `chore:`).
- Add tests for every behavior change.

## Pull Requests

- Describe problem and solution.
- Include compatibility impact.
- Update docs when behavior or API changes.
